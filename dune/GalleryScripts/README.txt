Gallery scripts.  No building is necessary, so no makefile is provided
(yet).  If we distribute built gallery programs, then we'd need to
add gallery as a product that dunetpc depends on.

To run them, setup dunetpc and gallery.  Follow the instructions
in each script to run them from the root command prompt.
